apiVersion: v1
kind: Service
metadata:
  name: {{ include "beep-website.fullname" . }}
  labels:
    {{- include "beep-website.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "beep-website.selectorLabels" . | nindent 4 }}
